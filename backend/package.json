{
  "name": "backend-usuarios",
  "version": "1.0.0",
  "description": "API REST com json-server para gerenciamento de usuários",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "dev": "json-server --watch db.json --port 3000 --delay 500"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
